<nav class="navbar-default navbar-static-side" role="navigation" style="position: fixed">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
          <li class="nav-header">
              <div class="dropdown profile-element">
                  <span>
                      <img alt="image" class="img" style="width:100%" src="<%= image_url 'dashboard/logo_transparent2.png' %>"/>
                  </span>
                  <% if user_signed_in? %>
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                            <span class="block m-t-xs">
                              <strong class="font-bold"><%= current_user.first_name %> <%= current_user.last_name %></strong>
                            </span>
                        </span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        <li><a href="">Profile</a>
                        </li>
                        <li><a href="">Contacts</a>
                        </li>
                        <li><a href="">Mailbox</a>
                        </li>
                        <li class="divider"></li>
                        <li>
                          <%= link_to destroy_user_session_path, method: :delete do %>
                            <i class="fa fa-sign-out"></i> Log out
                          <% end %>
                        </li>
                    </ul>
                  <% end %>

              </div>
              <div class="logo-element">
                  <img alt="image" class="img" style="width:100%" src="<%= image_url 'dashboard/logo_transparent.png' %>"/>
              </div>
            </li> 

            <% if user_signed_in? %>
              <%= render_navigation( context: :dashboard, renderer: DashboardRenderer, expand_all: true ) %> 
            <% end %> 
          </ul> 
    </div>
</nav>
